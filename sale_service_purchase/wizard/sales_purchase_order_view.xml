<odoo>
    <data>

        <record model="ir.ui.view" id="create_purchase_order_form_view">
            <field name="name">Sales Purchase Order</field>
            <field name="model">sales.purchase.order</field>
                <field name="arch" type="xml">
                    <form string="Purchase order for Services">
                        <label string="This wizard will create purchase order from sales order for services type of product."/>
                        <group>
                            <group>
                                <field name="partner_id" domain="[('supplier','=',True)]"/>
                            </group>
                            <group/>
                        </group>
                        <footer>
                            <button name="create_purchase_order" type="object" string="Criar PDC" class="btn-primary"/>
                            <button name="create_purchase_requisition" type="object" string="Criar Concorrência" class="btn-primary"/>
                            or
                            <button string="_Cancel" class="btn-default" special="cancel" />
                        </footer>
                    </form>
                </field>
        </record>

        <act_window id="action_create_purchase_order"
                    name="Sales Purchase Order"
                    res_model="sales.purchase.order"
                    view_mode="form"
                    target="new"
                    key2="client_action_multi"/>
        
        
   </data>
</odoo>
