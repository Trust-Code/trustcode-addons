<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_purchase_uom" model="ir.ui.view">
        <field name="name">product.template.purchase.uom</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <field name="uom_po_id" position="after" >
                <field name="purchase_uom_id" groups="product.group_uom"/>
                <field name="show_conversion_rate" invisible="1" groups="product.group_uom"/>
                <field name="conversion_rate" attrs="{'invisible': [('show_conversion_rate', '=', False)]}"  groups="product.group_uom"/>
                <p attrs="{'invisible': [('show_conversion_rate', '=', False)]}" class="oe_grey" colspan="2" groups="product.group_uom">
                    Ex: Fator de Conversão * Unidade de Medida de Compra = 2ª Unidade de Compra
                </p>
            </field>
            <!-- <field name="uom_po_id" position="attributes" >
                <attribute name="invisible">1</attribute>
            </field> -->
        </field>
    </record>
</odoo>